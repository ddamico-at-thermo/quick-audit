**Quick Audit** is a tool to speed the workflow of checking a list of URLs for target elements, with a split-screen view of the browser window. 
<P>
This is an accessible version with browser **voice control** toggle option.
<P>
Simply toggle on the Voice mode on the left sidebar and use the preset commands to navigate the workflow in Yes/No mode: 
<br>
<b>Yes</b> to mark the entry with a 'yes' in the action column and auto-advance to next entry. 
<br>
<b>No</b>to mark the entry with a 'no' in the action column and auto-advance to next entry.
<br>
<b>Scroll</b> to begin a brisk auto-scroll of the page frame. 
<br>
<b>Pause</b> to stop the auto-scroll command. 
<br>
<b>Next</b> to advance to next entry. 
<P>
When using the custom notes workflow: 
<br>
<b>Scroll</b> to begin a brisk auto-scroll of the page frame. 
<br>
<b>Pause</b> to stop the auto-scroll command. 
<br>
<b>Next</b> to advance to next entry. 
<br>
To add a <b>voice note</b>, simply start saying the note aloud and the tool will record it at the top right of the window. No command is necessary to trigger this feature, aside from toggling to "Notes" workflow mode. 
